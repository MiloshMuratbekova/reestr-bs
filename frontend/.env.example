# Базовый адрес API.
# В продуктивной сборке nginx проксирует /api на бэкенд, поэтому значение
# по умолчанию («/api») менять не нужно.
VITE_API_BASE=/api

# Используется только режимом разработки (npm run dev) для проксирования.
VITE_BACKEND_URL=http://10.10.31.35:8000
